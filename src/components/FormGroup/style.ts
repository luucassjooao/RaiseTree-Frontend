import styled from 'styled-components';

export const Container = styled.div`

  .form-item {
    position: relative;
    .loader {
      position: absolute;
      right: 28px;
      top: 28px;
    }
  }
`;
