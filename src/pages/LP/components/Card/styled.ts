import styled from 'styled-components';

export const StyledCard = styled.div`
  padding: 32px 44px;
  flex: 1;
  border: 2px solid #fff;

  /* img {
    display: none;
  }

  &:hover {
    padding: 0;

    img {
      display: flex;
      width: 100%;
      height: 100%;
    }

    h1 {
      display: none;
    }
  } */
`;
